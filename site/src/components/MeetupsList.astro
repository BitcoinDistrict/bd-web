---
import type { Meetup } from "../data/meetups";

export type Props = {
  meetups: Meetup[];
};

const { meetups } = Astro.props;
---

<div class="meetups">
  <ul class="meetups__grid">
    {meetups.map((meetup) => (
      <li class="meetups__card">
        <a class="meetups__link" href={meetup.meetupUrl} target="_blank" rel="noopener noreferrer">
          <div class="meetups__header">
            <div class="meetups__logoWrap">
              <img
                class="meetups__logo"
                src={meetup.logo.src}
                width={meetup.logo.width}
                height={meetup.logo.height}
                alt={`${meetup.title} logo`}
                loading="lazy"
              />
            </div>
            <div class="meetups__titles">
              <div class="meetups__title">{meetup.title}</div>
              <div class="meetups__location">{meetup.location.name}</div>
            </div>
          </div>

          <p class="meetups__description">{meetup.description}</p>

          <div class="meetups__meta">
            <span class="meetups__cta">View on Meetup →</span>
          </div>
        </a>
      </li>
    ))}
  </ul>
</div>

<style>
  .meetups__grid {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: 1fr;
    gap: 1.25rem;
  }

  @media (max-width: 768px) {
    .meetups__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .meetups__grid {
      grid-template-columns: 1fr;
    }
  }

  .meetups__card {
    display: flex;
    min-height: 0;
  }

  .meetups__link {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 1rem;
    border-radius: 16px;
    background: #fff;
    border: 1px solid rgba(0, 0, 0, 0.08);
    text-decoration: none;
    color: inherit;
    transition:
      transform 180ms ease,
      box-shadow 180ms ease,
      border-color 180ms ease;
  }

  .meetups__link:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.08);
    border-color: rgba(0, 0, 0, 0.14);
  }

  .meetups__header {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    margin-bottom: 0.75rem;
  }

  .meetups__logoWrap {
    width: 52px;
    height: 52px;
    flex: 0 0 auto;
    border-radius: 12px;
    overflow: hidden;
    background: #fff;
    display: grid;
    place-items: center;
  }

  .meetups__logo {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    display: block;
  }

  .meetups__title {
    font-weight: 700;
    font-size: 1.05rem;
    line-height: 1.2;
  }

  .meetups__location {
    margin-top: 0.15rem;
    color: rgba(0, 0, 0, 0.6);
    font-size: 0.92rem;
  }

  .meetups__description {
    margin: 0 0 0.85rem 0;
    color: rgba(0, 0, 0, 0.78);
    line-height: 1.55;
    font-size: 0.95rem;
    flex: 1;
  }

  .meetups__meta {
    display: flex;
    gap: 0.75rem;
    justify-content: flex-end;
    align-items: baseline;
    flex-wrap: wrap;
    margin-top: auto;
  }

  .meetups__cta {
    font-weight: 600;
    font-size: 0.92rem;
    color: rgba(0, 0, 0, 0.9);
  }
</style>

